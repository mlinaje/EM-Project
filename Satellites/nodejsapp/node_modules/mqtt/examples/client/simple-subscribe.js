var mqtt = require('../..')
  , host = '127.0.0.1' // or localhost
  , client = mqtt.connect();

client.subscribe('mqtt-transport/#');
client.on('message', function(topic, message) {
  console.log(message);
});
